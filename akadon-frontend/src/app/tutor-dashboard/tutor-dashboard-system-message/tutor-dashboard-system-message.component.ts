import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-tutor-dashboard-system-message',
  templateUrl: './tutor-dashboard-system-message.component.html',
  styleUrls: ['./tutor-dashboard-system-message.component.css'],
})
export class TutorDashboardSystemMessageComponent implements OnInit {
  constructor() {}
  // result: number = 10;
  // listMessage: any;
  // message: Message = {};
  // viewMessage(event: any) {
  //   var messageId = event.currentTarget.id;
  //   messageId = messageId.substring(8, messageId.length);
  //   this.messageService.viewMessage(messageId).subscribe((res) => {});
  //   this.messageService.getById(messageId).subscribe((res) => {
  //     this.message = res;
  //   });
  // }
  ngOnInit(): void {}
}
